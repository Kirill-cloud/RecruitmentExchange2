<UserControl x:Class="RecruitmentExchange.View.AddAplicantControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RecruitmentExchange.View" xmlns:viewmodel="clr-namespace:RecruitmentExchange.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:DealsVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel x:Name="StackTest" >
            <Label Content="да" />
            <TextBox Text="Да" />
            <Label Content="да" />
            <TextBox Text="Да" />
            <Label Content="да" />
            <TextBox Text="Да" />
            <Button Command="{Binding ShowDeals}" Content="Добавить" />
        </StackPanel>
    </Grid>
</UserControl>
